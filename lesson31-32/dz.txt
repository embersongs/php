1. В Models добавьте остальные модели из предыдущего проекта, распишите все поля (user_id)
2. В index создайте экзепляры этих моделей и вызовите у них методы.
3. Отдельно от проекта сделайте иерархию классов:
    а) Сделайте абстрактный класс Product и абстрактным методом подсчета стоимости товара.
    б) Сделайте наследников, физический товар (цена+доставка) и цифровой товар (цена) и реализуйте в каждом свой алгоритм подсчета стоимости.
        (в физическом товаре учтите стоимость доставки-упаковки)

        Сделайте свойства, конструкторы, и пример работы.

4* В class Db реализуйте метод query($sql), который просто вернет текст запроса, а в модели задействуйте этот класс и метод.