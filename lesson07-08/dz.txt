Функции в консоли https://www.php.net/manual/ru/ref.array.php
1. Подготовьте массив целых чисел (4, 5, 1, 4, 7, 8, 15, 6, 71, 45, 2)
Сделайте функцию, которая по этому массиву вернет новый массив, в котором каждое значение
будет заменено строкой "четное" или "не четное". ["четное", "не четное"...] evenOdd()
Сделать через цикл
*Сделать через array_map

2. Разработайте функцию aggregationData() с объявленными типами аргументов и возвращаемого значения,
принимающую в качестве аргумента массив целых чисел.
$mas = [4, 5, 1, 4, 7, 8, 15, 6, 71, 45, 2];
Результатом работы функции должен быть массив, содержащий три элемента:
max — наибольшее число, min — наименьшее число, avg — среднее арифметическое всех чисел массива.
не использовать цикл!

3. Используя mt_rand, сгенерируйте массив размером 100 элементов так,
 чтобы все элементы были в диапазоне от 1 до 200 УНИКАЛЬНЫЕ
Используйте цикл.
*Используйте только функции работы с массивами

4*. Дан массив $mas = [4, 5, 1, 4, 7, 8, 15, 6, 71, 45, 2]; сделайте алгоритм и найдите сумму между
минимальным и максимальными значениями. Не используя готовые функции. Используя цикл.
** Используйте один цикл, в один проход.

Работа с html
доработайте блог
- добавьте обработку ошибок чтения файла (выводите сообщение ошибка 500)
- вынесите из шаблона меню в components
- сделайте 2 функции для чтения всех постов и одного поста по $id в файле functions
***
- добавьте файл categories.json  с категориями постов, сделайте страницу категории, где выводятся категории постов (спорт, политика), при клике на категорию отображаются только посты этой категории.