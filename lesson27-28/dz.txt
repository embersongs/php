1. Реализовать блог на функциях на своем уровне понимания, варианты:
    а) каждая страница и все действия на ней это один файл, и код и шаблон.
    б) код разбит на файлы по смыслу
    с) реализована БД и ЧПУ, архитектура строго MVC, есть контроллеры

Требуемый функционал:
    а) отображение постов, одного поста, категорий и постов категории. CRUD одного ресурса (посты)
    б) реализована авторизация и править можно только свой пост.
    с) есть загрузка и отображение изображений

Задание на 3 уровня сложности: а), б), с). Зачту даже только а), но нужно пробовать делать дальше.